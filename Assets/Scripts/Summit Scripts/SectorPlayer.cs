using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SectorPlayer : MonoBehaviour
{
    // Start is called before the first frame update
    public List<Component> ComponentToDestroy = new List<Component>();
}
